import LocalFileUploader from './upload-file'

describe('[LocalFileUploader]', () => {
  const sut = new LocalFileUploader()

  it('should respond with "Hello, World!"', () => {
    expect(true).toEqual(true)
  })
})
